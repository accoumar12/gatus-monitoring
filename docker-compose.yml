services:
  gatus:
    image: twinproduction/gatus:v5.33.1
    ports:
      - "7777:8080"
    volumes:
      - ./config:/config
      - ./data:/data/
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=Europe/London

